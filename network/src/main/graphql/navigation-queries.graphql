query NavEntriesByHandle($handle: String!) {
    navigation(handle: $handle) {
        items {
            ...NavMenuItemContainer
        }
        attributes {
            ...AttributesInfo
        }
    }
}
