query GetProductDetails($handle: String!) {
    productDetails(handle: $handle) {
        ...ProductFragment
    }
}

query GetRelatedProducts($handle: String!, $limit: Int!) {
    relatedProducts(handle: $handle, limit: $limit) {
        ...ProductListEntryFragment
    }
}
