package au.com.alfie.ecomm.designsystem.component.snackbar

/**
 * Defines the priority that the snackbar should have in case there is other snackbar displayed
 */
enum class SnackbarPriority {
    HIGH,
    NORMAL
}
