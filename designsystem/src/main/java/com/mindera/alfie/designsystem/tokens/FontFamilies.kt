package com.mindera.alfie.designsystem.tokens

import androidx.compose.ui.text.font.FontFamily

// TODO(fonts): replace with real font resources (Google Fonts or bundled assets) when available.
internal val FontFamilyBrand = FontFamily.Default   // Libre Baskerville — serif display
internal val FontFamilySystem = FontFamily.Default  // Roboto — sans-serif body/UI
