# Minification is off for every build type today (see app/build.gradle.kts). When it is turned
# on, ML Kit's model loading reflects over these classes and needs:
#   -keep class com.google.mlkit.** { *; }
